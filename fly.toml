app = "dancodes"
primary_region = "iad"

[build]

[env]
  XDG_DATA_HOME = "/vol/opencode-state"
  OPENCODE_CONFIG = "/opt/dancodes/opencode.json"
  GITHUB_USER = "jdanbrown"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [http_service.concurrency]
    type = "connections"
    hard_limit = 100
    soft_limit = 80

[[vm]]
  size = "performance-8x"
  memory = "16gb"

[mounts]
  source = "dancodes_vol"
  destination = "/vol"
